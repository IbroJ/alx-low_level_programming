#include <stdio.h>

/**
 * main - prints alphabet in lowercase
 * Return: Always 0
 */
int main(void)
{
	char lc;

	lc = 'a';
	while
		(lc <= 'z') {
			putchar(lc);
			lc++;
		}
	putchar('\n');
	return (0);
}
